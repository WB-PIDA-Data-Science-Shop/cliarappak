% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_downloads.R
\name{trends_check_data}
\alias{trends_check_data}
\title{Does a country have gaps in an indicator over a year range?}
\usage{
trends_check_data(start_year, end_year, country, var, raw_data)
}
\arguments{
\item{start_year, end_year}{Numeric. Inclusive bounds of the year range to
check.}

\item{country}{Character. The comparison-country name to check.}

\item{var}{Character. Indicator \emph{code} (column name in \code{raw_data}).}

\item{raw_data}{Wide indicator panel with \code{country_name} / \code{Year} columns
(\code{app_data$raw_data}).}
}
\value{
A single logical: \code{TRUE} if any year in the range has an \code{NA} for
\code{var} (exclude this country), \code{FALSE} if the series is complete.
}
\description{
The Time Trends tab uses this to narrow the list of comparison countries to
those with a complete series for the chosen indicator over the base
country's observed year span.
}
\seealso{
\code{\link[=check_data]{check_data()}}, \code{\link[=trends_plot]{trends_plot()}}, \code{\link[=mod_trends_server]{mod_trends_server()}}.
}
