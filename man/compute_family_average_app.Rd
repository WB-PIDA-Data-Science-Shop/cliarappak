% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_family.R
\name{compute_family_average_app}
\alias{compute_family_average_app}
\title{Family-average closeness-to-frontier columns (\verb{*_avg})}
\usage{
compute_family_average_app(
  cliar_data,
  vars,
  type = "static",
  db_variables,
  base_country,
  comparison_countries
)
}
\arguments{
\item{cliar_data}{Wide closeness-to-frontier table -- \code{app_data$global_data}
(static) or \code{app_data$global_data_dyn} (dynamic).}

\item{vars}{Character vector of indicator codes to aggregate.}

\item{type}{One of \code{"static"} or \code{"dynamic"}.}

\item{db_variables}{Indicator dictionary (\code{app_data$db_variables}).}

\item{base_country}{Character vector of base-country name(s); drives the
missing-data screen.}

\item{comparison_countries}{Character vector of comparison-country names;
used with \code{base_country} for the low-variance screen.}
}
\value{
A wide tibble of \verb{<family>_avg} columns keyed by \code{country_code}
(and \code{year}, when \code{type = "dynamic"}), as returned by
\code{\link[cliaretl:compute_family_average]{cliaretl::compute_family_average()}}.
}
\description{
Produces the per-family average closeness-to-frontier columns (named
\verb{<family>_avg}) that the benchmarking plots draw as the family-level markers.
This is a thin app-side wrapper: it applies the app's missing-data and
low-variance pre-filtering, then hands off to
\code{\link[cliaretl:compute_family_average]{cliaretl::compute_family_average()}} for the actual aggregation, so the
dashboard and the \code{cliaretl} pipeline stay in lock-step on how families are
averaged.
}
\details{
Pre-filtering removes indicators the base country has no data for (any \code{NA})
and indicators that are flat across the base + comparison countries
(\code{\link[=check_quantiles]{check_quantiles()}}), then the same names are dropped from \code{vars}. The
delegated call passes \code{require_complete = FALSE} and \code{exclude_pattern = "gdp"}. The \code{type} argument selects a static (one period) or dynamic
(per-year) aggregation and is forwarded unchanged.
}
\seealso{
\code{\link[=family_data]{family_data()}}, \code{\link[=def_quantiles]{def_quantiles()}},
\code{\link[cliaretl:compute_family_average]{cliaretl::compute_family_average()}}.
}
