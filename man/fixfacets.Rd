% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_plots.R
\name{fixfacets}
\alias{fixfacets}
\title{Equalise facet widths in a facetted ggplotly}
\usage{
fixfacets(figure, facets, domain_offset)
}
\arguments{
\item{figure}{A \code{plotly} object from \code{plotly::ggplotly()} of a facetted
ggplot.}

\item{facets}{Character vector of facet (strip) labels, in display order.}

\item{domain_offset}{Numeric. Width of the strip-label shape as a fraction of
the plot width (e.g. \code{0.16}).}
}
\value{
The \code{figure}, with panel domains and strip positions rewritten.
}
\description{
\code{plotly::ggplotly()} renders the first and last panels of a \code{facet_wrap()}
wider than the middle ones. \code{fixfacets()} rewrites the panel domains, the
grey strip-label background shapes, and the strip-label annotation positions
so every facet is the same width.
}
\details{
Adapted from
\url{https://stackoverflow.com/questions/61580973/}. Strip backgrounds are
identified by their fill colour \code{"rgba(217,217,217,1)"}; strip labels by
matching their text against \code{facets}. Currently called only from
commented-out code, but kept as a utility for future facetted plotly output.
}
\seealso{
\code{\link[=clean_plotly_legend]{clean_plotly_legend()}}.
}
