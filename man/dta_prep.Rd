% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_downloads.R
\name{dta_prep}
\alias{dta_prep}
\alias{rds_prep}
\alias{csv_prep}
\title{Prepare the download table for a given file format}
\usage{
dta_prep(data, des_names = FALSE, db_variables)

rds_prep(data, des_names, db_variables)

csv_prep(data, des_names, db_variables)
}
\arguments{
\item{data}{The wide closeness-to-frontier table to export.}

\item{des_names}{Logical. \code{TRUE} renames indicator-code columns to their
human-readable \code{var_name}s; \code{FALSE} (the \code{dta_prep()} default) keeps the
codes.}

\item{db_variables}{Indicator dictionary (\code{app_data$db_variables}) providing
the \code{variable} -> \code{var_name} mapping used when \code{des_names = TRUE}.}
}
\value{
A \code{data.table} copy of \code{data} with cleaned (and, for \code{dta_prep()},
truncated) unique column names.
}
\description{
The Data tab lets a user download the closeness-to-frontier table as Stata
(\code{dta_prep()}), RDS (\code{rds_prep()}) or CSV (\code{csv_prep()}). Each function takes
the shared in-memory table and returns a copy with column names cleaned to
what that format needs.
}
\details{
All three: take a \code{data.table::copy()} first (\pkg{data.table}'s
\code{setnames()} mutates by reference, so without the copy the shared
\code{pre_download_data()} reactive would be corrupted for the next format the
user downloads in the same session); optionally swap indicator codes for
display names; de-duplicate; then replace every character that is not
\verb{[A-Za-z0-9_]} with \verb{_}.

They differ only in how far names are truncated:

\describe{
\item{\code{dta_prep()}}{display names truncated to 32 chars on rename, column
names truncated to 30 before \code{make.unique()} (Stata's variable-name
limit, with headroom for the \code{make.unique()} suffix).}
\item{\code{rds_prep()}}{display names truncated to 32 chars on rename; no
further truncation.}
\item{\code{csv_prep()}}{no truncation at all -- full display names kept.}
}
}
\seealso{
\code{\link[=make_colnames_unique]{make_colnames_unique()}}, \code{\link[=mod_data_server]{mod_data_server()}}.
}
