% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_app_data.R
\name{build_app_data}
\alias{build_app_data}
\title{Build the CLIAR app's shared data objects}
\usage{
build_app_data(dynamic_year_cutoff = NULL)
}
\arguments{
\item{dynamic_year_cutoff}{Integer year, or \code{NULL} (default) to derive one
via \code{\link[=resolve_dynamic_year_cutoff]{resolve_dynamic_year_cutoff()}}.}
}
\value{
A named list of the data objects shared across \code{cliarappak}'s
modules (the \code{app_data} argument they all take). Elements:
\describe{
\item{\code{db_variables}}{indicator dictionary, filtered to benchmarked /
family-level rows.}
\item{\code{vars_all}, \code{vars_family}}{character vectors of indicator and
family-variable codes.}
\item{\code{definitions}}{nested per-family indicator definitions for the
methodology / definition tables.}
\item{\code{raw_data}}{wide indicator panel (\code{Year} column), 1990+.}
\item{\code{global_data}, \code{global_data_dyn}}{wide closeness-to-frontier
tables, static and dynamic (dynamic filtered to \code{dynamic_year_cutoff}).}
\item{\code{ctf_long}, \code{ctf_long_dyn}}{the same, pivoted long.}
\item{\code{country_groups}, \code{country_list}, \code{family_order}}{\code{cliaretl}
metadata tables.}
\item{\code{spatial_data}}{\code{sf} layer for the World Map tab (Robinson
projection).}
\item{\code{family_names}, \code{variable_names}}{family and indicator name
lookups.}
\item{\code{countries}, \code{flags_with_countries}}{sorted country names, and the
same with flag \verb{<img>} markup for the pickers.}
\item{\code{variable_list}, \code{variable_list_benchmarked},
\code{filtered_variable_list}, \code{y_scatter_choices}}{grouped picker choice
lists.}
\item{\code{group_list}, \code{all_groups}}{comparison-group choices.}
\item{\code{plot_height}}{base plot height in px (500).}
\item{\code{dynamic_year_cutoff}}{the resolved cutoff year.}
}
}
\description{
Ports the "Auxiliary functions" / "Data" / "Options" sections of the
original \code{cliarapp}'s \code{global.R} (and the \code{vars-control.R} file it
sourced) into one function, computed once and returned as a named list.
}
\details{
In the original script app these were implicit globals available to every
tab because everything ran in one \code{global.R} + flat \code{server()} scope. Per
the migration's Tier-1 rule, \code{cliarappak} never relies on that: \code{app_ui()}
and \code{app_server()} each call this once and pass the pieces each module
needs as explicit arguments.

Two intentional deviations from a literal line-by-line port, both because
the original behavior was found to be dead code, not a design to preserve:
\itemize{
\item \code{global.R:38-45} joined \code{cliaretl::family_order} onto \code{db_variables}, but
\code{vars-control.R} (sourced immediately after) reassigns \code{db_variables <- cliaretl::db_variables_final} before anything reads the joined columns,
so the join was never observable. Not reproduced here. \code{family_order}
itself is still kept in the returned list, unjoined -- \code{server.R:1358}
(the benchmark "Overview" plot) uses it directly via its own
\code{left_join()}/\code{arrange()}, independent of the dead \code{db_variables} join.
\item \code{vars-control.R} computed 14 per-family \verb{vars_*} vectors
(\code{vars_anticorruption}, \code{vars_mkt}, ...) and several \verb{vars_*_ctf}
benchmarking vectors. Grepping \code{server.R}/\code{ui.R} for each name found zero
references outside the file that defined them — dead. Only \code{vars_all}
and \code{vars_family}, which genuinely are used downstream, are kept.
}

The original app also bundled a static \code{coverage_ctf_for_analysis.csv}
(→ \code{year_ctf_dynamic}), used only by the Coverage Report. That file was
never sourced from \code{cliaretl} at runtime -- it was the \emph{output} of a
one-off script (\code{cliarapp/source/data_coverage_processing.R}) that
someone had to remember to re-run and re-copy in by hand each data-release
cycle. It's not shipped here at all; see \code{\link[=prepare_app_data_coverage]{prepare_app_data_coverage()}},
which computes the same thing live and is called lazily from
\code{mod_reports.R}'s Coverage Report download handler (not from here) since
it's only needed for that one report, not every session.

\code{global_data_dyn} (the dynamic-benchmarking / Time Trends dataset) used to
hardcode \code{filter(year <= 2024)} -- a number that silently hid any new year
\code{cliaretl} added until someone edited this file by hand. \code{dynamic_year_cutoff}
replaces that: \code{NULL} (the default) derives it from \code{cliaretl::db_variables}'s
own \code{ref_year} attribute via \code{\link[=resolve_dynamic_year_cutoff]{resolve_dynamic_year_cutoff()}}, so it tracks
each \code{cliaretl} data-release cycle automatically. Pass an explicit year to
preview a different vintage -- this is also how \code{run_app()}'s own
\code{dynamic_year_cutoff} argument reaches this function.
}
