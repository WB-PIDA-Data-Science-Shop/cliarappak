% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_quantiles.R
\name{low_variance}
\alias{low_variance}
\alias{low_variance_dyn}
\title{Indicator codes that are flat within the comparison group}
\usage{
low_variance(
  data,
  base_country,
  country_list,
  comparison_countries,
  vars,
  variable_names
)

low_variance_dyn(
  data,
  base_country,
  country_list,
  comparison_countries,
  vars,
  variable_names
)
}
\arguments{
\item{data}{Long/wide CLIAR indicator table -- \code{app_data$global_data} for the
static variant, \code{app_data$global_data_dyn} for the dynamic one. First five
columns are identifiers (\code{country_name}, \code{country_code}, region/income
grouping, etc.); the remaining columns are one indicator each.}

\item{base_country}{Character. One or more country names. The country (or
countries) being benchmarked; drives both the missing-data and
low-variance screens.}

\item{country_list}{Data frame of country metadata with at least a
\code{country_name} column. Used to resolve \code{comparison_countries} to the set of
rows to keep. This is \code{app_data$country_list}.}

\item{comparison_countries}{Character vector of country names to benchmark
the base country against.}

\item{vars}{Character vector of indicator \emph{codes} (the \code{variable} column, not
the display name) to evaluate.}

\item{variable_names}{Indicator dictionary: a data frame keyed by \code{variable}
with \code{var_name} (display label) and \code{family_var} / family columns. This is
\code{app_data$variable_names}.}
}
\value{
Character vector of indicator codes (\code{variable} values) with zero
interquartile spread for the base country. \code{low_variance()} may contain
duplicates; \code{low_variance_dyn()} is de-duplicated. Returned via an explicit
\code{return()}.
}
\description{
\code{low_variance()} returns the indicator \emph{codes} for which the base country's
25th and 75th percentiles (computed across the base + comparison countries)
are identical -- i.e. the indicator has no spread and so carries no
benchmarking signal. \code{\link[=def_quantiles]{def_quantiles()}} applies the same screen internally to
drop rows; this function exposes the list so callers (e.g.
\code{mod_benchmark_server()}) can also show it in the plot notes.
}
\details{
\code{low_variance_dyn()} is the dynamic-dataset variant: it uses the "100\%
missing" rule for the missing-data pre-filter (matching \code{\link[=def_quantiles_dyn]{def_quantiles_dyn()}})
and de-duplicates the result.

The screen is computed on the long, dictionary-joined table exactly as in
\code{\link[=def_quantiles]{def_quantiles()}}, using \code{threshold}-independent quartiles (\code{q25} / \code{q75}).
Only indicators the base country actually has data for are considered; unlike
\code{\link[=def_quantiles]{def_quantiles()}}, these functions do \strong{not} exempt \verb{_avg} family-average
columns.
}
\seealso{
\code{\link[=def_quantiles]{def_quantiles()}}, \code{\link[=missing_var]{missing_var()}}, \code{\link[=plot_notes_function]{plot_notes_function()}}.
}
