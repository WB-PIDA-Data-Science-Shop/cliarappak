% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_plots.R
\name{static_plot_dyn}
\alias{static_plot_dyn}
\title{Dynamic (year-by-year) benchmarking plot}
\usage{
static_plot_dyn(
  data,
  base_country,
  tab_name,
  rank,
  group_median = NULL,
  custom_df = NULL,
  title = TRUE,
  dots = FALSE,
  note = NULL,
  threshold,
  preset_order = FALSE,
  db_variables,
  ctf_long_dyn
)
}
\arguments{
\item{data}{Long tibble from \code{\link[=def_quantiles_dyn]{def_quantiles_dyn()}} (carries a \code{year} column).}

\item{base_country}{Character vector of base-country name(s).}

\item{tab_name}{Character. The institutional family being shown, or
\code{"Overview"}. Drives y-axis ordering and a few layout tweaks.}

\item{rank}{Logical. \code{TRUE} plots percentile rank (\code{dtt}); \code{FALSE} plots the
raw closeness-to-frontier value (\code{dtf}).}

\item{group_median}{Character vector of comparison-group names whose medians
should be drawn, or \code{NULL}.}

\item{custom_df}{Data frame of user-defined comparison groups
(\code{Grp} / \code{Countries} columns) from \code{mod_benchmark_server()}, or \code{NULL}.}

\item{title}{Logical. Whether to draw the plot title.}

\item{dots}{Logical. Whether to overlay individual comparison-country points.}

\item{note}{Character note to print under the plot, or \code{NULL}.}

\item{threshold}{One of \code{"Default"} or \code{"Terciles"} -- must match the value
passed to \code{\link[=def_quantiles]{def_quantiles()}}; sets the \code{"Weak"/"Emerging"/"Strong"} factor
levels and legend cut points.}

\item{preset_order}{Logical. \code{TRUE} orders indicators by the base country's
own value/rank; \code{FALSE} uses \code{db_variables$rank_id}.}

\item{db_variables}{Indicator dictionary (\code{app_data$db_variables}) -- supplies
\code{rank_id} for indicator ordering.}

\item{ctf_long_dyn}{Long dynamic closeness-to-frontier table
(\code{app_data$ctf_long_dyn}) used for the group-median markers.}
}
\value{
A \code{ggplot} object.
}
\description{
The dynamic-dataset counterpart of \code{\link[=static_plot]{static_plot()}}: same figure, but each
indicator is faceted/labelled by \code{indicator : year} so the user can see how
the base country's position moved over time. Consumes the output of
\code{\link[=def_quantiles_dyn]{def_quantiles_dyn()}}.
}
\details{
Indicators with only one year of data for the base country are dropped (a
trend needs at least two points). \code{ctf_long_dyn} is joined to \code{db_variables}
for display names and filtered to the same \code{indicator : year} combinations
present for the base country.
}
\seealso{
\code{\link[=static_plot]{static_plot()}}, \code{\link[=def_quantiles_dyn]{def_quantiles_dyn()}}, \code{\link[=interactive_plot]{interactive_plot()}}.
}
