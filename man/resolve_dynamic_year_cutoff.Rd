% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_app_data.R
\name{resolve_dynamic_year_cutoff}
\alias{resolve_dynamic_year_cutoff}
\title{Resolve the dynamic-benchmarking year cutoff}
\usage{
resolve_dynamic_year_cutoff(dynamic_year_cutoff = NULL)
}
\arguments{
\item{dynamic_year_cutoff}{Integer year, or \code{NULL} to derive one from
\code{cliaretl::db_variables}'s \code{ref_year} attribute (\code{ref_year - 1}) --
e.g. \code{ref_year = 2025} resolves to \code{2024}.}
}
\value{
A single integer year.
}
\description{
The single place that turns "no explicit cutoff given" into an actual
year, so \code{\link[=build_app_data]{build_app_data()}}, \code{\link[=run_app]{run_app()}}, and any future deploy helper
that needs to bake a specific cutoff into a deployment all agree on the
same default without duplicating the logic.
}
