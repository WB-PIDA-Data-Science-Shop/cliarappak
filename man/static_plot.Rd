% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_plots.R
\name{static_plot}
\alias{static_plot}
\title{Static benchmarking dot/point plot (Country Benchmarking tab)}
\usage{
static_plot(
  data,
  base_country,
  tab_name,
  rank,
  group_median = NULL,
  custom_df = NULL,
  title = TRUE,
  dots = FALSE,
  note = NULL,
  threshold,
  preset_order = FALSE,
  report = FALSE,
  db_variables,
  family_order,
  ctf_long
)
}
\arguments{
\item{data}{Long tibble from \code{\link[=def_quantiles]{def_quantiles()}} / \code{\link[=family_data]{family_data()}}, filtered to
the base and comparison countries, with \code{var_name}, \code{dtf}, \code{dtt}, \code{status},
\code{variable} columns.}

\item{base_country}{Character vector of base-country name(s).}

\item{tab_name}{Character. The institutional family being shown, or
\code{"Overview"}. Drives y-axis ordering and a few layout tweaks.}

\item{rank}{Logical. \code{TRUE} plots percentile rank (\code{dtt}); \code{FALSE} plots the
raw closeness-to-frontier value (\code{dtf}).}

\item{group_median}{Character vector of comparison-group names whose medians
should be drawn, or \code{NULL}.}

\item{custom_df}{Data frame of user-defined comparison groups
(\code{Grp} / \code{Countries} columns) from \code{mod_benchmark_server()}, or \code{NULL}.}

\item{title}{Logical. Whether to draw the plot title.}

\item{dots}{Logical. Whether to overlay individual comparison-country points.}

\item{note}{Character note to print under the plot, or \code{NULL}.}

\item{threshold}{One of \code{"Default"} or \code{"Terciles"} -- must match the value
passed to \code{\link[=def_quantiles]{def_quantiles()}}; sets the \code{"Weak"/"Emerging"/"Strong"} factor
levels and legend cut points.}

\item{preset_order}{Logical. \code{TRUE} orders indicators by the base country's
own value/rank; \code{FALSE} uses \code{db_variables$rank_id}.}

\item{report}{Logical. \code{TRUE} when rendering into the downloadable report
(tweaks sizing/labels).}

\item{db_variables}{Indicator dictionary (\code{app_data$db_variables}) -- supplies
\code{rank_id} for indicator ordering.}

\item{family_order}{Data frame mapping \code{family_name} to a numeric
\code{family_order} (\code{app_data$family_order}); used only on the Overview tab.}

\item{ctf_long}{Long closeness-to-frontier table (\code{app_data$ctf_long}) used
to position the group-median markers.}
}
\value{
A \code{ggplot} object. Returned via an explicit \code{return()}.
}
\description{
Builds the ggplot behind the Country Benchmarking tab's main figure: one row
per indicator (or per institutional family, on the "Overview" tab), a
coloured point for the base country positioned at its closeness-to-frontier
value, and optionally the comparison-group median and individual comparison
countries. Piped into \code{\link[=interactive_plot]{interactive_plot()}} to become the plotly the user
sees.
}
\details{
\code{static_plot()} works on the static (single latest period) dataset;
\code{\link[=static_plot_dyn]{static_plot_dyn()}} is the year-by-year counterpart.

The \code{data} argument is the output of \code{\link[=def_quantiles]{def_quantiles()}} (or, for the Overview
tab, \code{\link[=family_data]{family_data()}} joined to \code{family_order}) -- it already carries the
\code{dtf}, \code{dtt}, \code{status} and \code{nrank} columns this function draws from. Indicator
ordering on the y-axis is either taken from \code{db_variables$rank_id}
(\code{preset_order = FALSE}) or from the base country's own ranking
(\code{preset_order = TRUE}). The word "Institutions" is stripped from y-axis
labels (issue #283).
}
\seealso{
\code{\link[=static_plot_dyn]{static_plot_dyn()}}, \code{\link[=interactive_plot]{interactive_plot()}}, \code{\link[=plot_notes_function]{plot_notes_function()}},
\code{\link[=def_quantiles]{def_quantiles()}}.
}
