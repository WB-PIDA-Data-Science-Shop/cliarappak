% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_plots.R
\name{trends_plot}
\alias{trends_plot}
\title{Time-series plot for one indicator (Time Trends tab)}
\usage{
trends_plot(
  raw_data,
  indicator,
  indicator_name,
  base_country,
  comparison_countries,
  country_list,
  groups,
  definitions,
  custom_df = NULL,
  base_color,
  comp_color,
  groups_color
)
}
\arguments{
\item{raw_data}{Wide indicator panel with a \code{Year} column
(\code{app_data$raw_data}).}

\item{indicator}{Character. Indicator code (column name in \code{raw_data}).}

\item{indicator_name}{Character. Indicator display name (for the title and
the \code{definitions} lookup).}

\item{base_country}{Character. Single base-country name.}

\item{comparison_countries}{Character vector of individual comparison-country
names.}

\item{country_list}{Country-to-group lookup (\code{app_data$country_list}) used to
resolve \code{groups} to their member countries.}

\item{groups}{Character vector of comparison-group names whose medians to
draw.}

\item{definitions}{Indicator dictionary (\code{app_data$db_variables}).}

\item{custom_df}{Custom-group data frame (\code{Grp} / \code{Countries}), or \code{NULL}.}

\item{base_color, comp_color, groups_color}{Hex colour strings for the base
country line, comparison-country lines, and group-median lines.}
}
\value{
A \code{plotly} htmlwidget.
}
\description{
Builds the interactive plotly line chart on the Time Trends tab: the base
country's value of one indicator year by year, overlaid with individual
comparison countries and comparison-group (or custom-group) medians. Unlike
the other plot builders this one returns plotly directly -- there is no
separate \verb{interactive_*} step.
}
\details{
The x-range is clipped to the years the base country actually has data for.
Group medians are computed across the group's members for each year;
custom groups (from \code{custom_df}) are handled the same way. Colours are taken
from the three \code{colourInput()} controls on the tab.
}
\seealso{
\code{\link[=trends_check_data]{trends_check_data()}}, \code{\link[=mod_trends_server]{mod_trends_server()}}.
}
