% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_quantiles.R
\name{def_quantiles}
\alias{def_quantiles}
\alias{def_quantiles_dyn}
\title{Classify indicators into strength tiers by within-comparison-group quantile}
\usage{
def_quantiles(
  data,
  base_country,
  country_list,
  comparison_countries,
  vars,
  variable_names,
  threshold
)

def_quantiles_dyn(
  data,
  base_country,
  country_list,
  comparison_countries,
  vars,
  variable_names,
  threshold
)
}
\arguments{
\item{data}{Long/wide CLIAR indicator table -- \code{app_data$global_data} for the
static variant, \code{app_data$global_data_dyn} for the dynamic one. First five
columns are identifiers (\code{country_name}, \code{country_code}, region/income
grouping, etc.); the remaining columns are one indicator each.}

\item{base_country}{Character. One or more country names. The country (or
countries) being benchmarked; drives both the missing-data and
low-variance screens.}

\item{country_list}{Data frame of country metadata with at least a
\code{country_name} column. Used to resolve \code{comparison_countries} to the set of
rows to keep. This is \code{app_data$country_list}.}

\item{comparison_countries}{Character vector of country names to benchmark
the base country against.}

\item{vars}{Character vector of indicator \emph{codes} (the \code{variable} column, not
the display name) to evaluate.}

\item{variable_names}{Indicator dictionary: a data frame keyed by \code{variable}
with \code{var_name} (display label) and \code{family_var} / family columns. This is
\code{app_data$variable_names}.}

\item{threshold}{One of \code{"Default"} (cut points at the 25th and 50th
percentiles) or \code{"Terciles"} (33rd and 66th). Controls both the \code{status}
labels and \code{q_cutoff1} / \code{q_cutoff2}.}
}
\value{
A tibble in long form, one row per country-indicator
(\code{def_quantiles_dyn()}: per country-indicator-year), with columns:
\code{country_name}, \code{variable}, \code{var_name} (plus the other dictionary columns),
\code{year} (dynamic only), \code{dtf} (the value), \code{dtt} (percentile rank),
\code{q_lv_25}, \code{q_lv_75}, \code{q_cutoff1}, \code{q_cutoff2}, \code{status}, \code{nrank}.
Returned invisibly.
}
\description{
\code{def_quantiles()} is the core benchmarking calculation behind the Country
Benchmarking tab. Given a base country and a set of comparison countries, it
ranks every selected indicator \emph{within that comparison group} and labels each
country's value as \code{"Weak"}, \code{"Emerging"}, or \code{"Strong"} according to where it
falls in the group's distribution.
}
\details{
\code{def_quantiles_dyn()} is the same calculation for the dynamic (year-by-year)
dataset: it additionally groups by \code{year}, so an indicator can be classified
differently in different years.

Steps, in order:
\enumerate{
\item \strong{Drop indicators the base country has no data for.} \code{def_quantiles()}
treats an indicator as missing if the base country has \emph{any} \code{NA} for it;
\code{def_quantiles_dyn()} uses the stricter rule of \emph{100\%} missing (so an
indicator with at least one observed year survives). Family-average
columns (names ending \verb{_avg}) are never dropped by this step.
\item \strong{Restrict to base + comparison countries and the surviving indicators},
then pivot long and join \code{variable_names} for display labels and family.
\item \strong{Compute, per indicator (\code{def_quantiles_dyn()}: per indicator-year):}
\itemize{
\item \code{dtt} -- \code{dplyr::percent_rank()} of the value (0-1),
\item \code{q_lv_25} / \code{q_lv_75} -- the 25th / 75th percentiles (used only for the
low-variance screen below),
\item \code{q_cutoff1} / \code{q_cutoff2} -- the percentile values at the two \code{threshold}
cut points,
\item \code{status} -- the \code{"Weak"} / \code{"Emerging"} / \code{"Strong"} label, and
\item \code{nrank} -- \code{dplyr::min_rank(-value)}, i.e. 1 = highest value in the group.
The original \code{value} column is renamed \code{dtf} ("distance to frontier").
}
\item \strong{Screen out low-variance indicators}: if the base country's 25th and
75th percentiles are equal, the indicator carries no comparative signal
and its rows are removed (again, \verb{_avg} columns are exempt). See
\code{\link[=low_variance]{low_variance()}} for the same screen exposed as a standalone list.
}

Both functions return their result \strong{invisibly} (the last statement is an
assignment), which is why callers either assign the result or pass it
straight into another call such as \code{\link[=family_data]{family_data()}}.
}
\seealso{
\code{\link[=low_variance]{low_variance()}} and \code{\link[=missing_var]{missing_var()}} for the screens used here
exposed on their own; \code{\link[=family_data]{family_data()}} and \code{\link[=compute_family_average_app]{compute_family_average_app()}}
for the family-level roll-up; \code{\link[=static_plot]{static_plot()}} / \code{\link[=static_plot_dyn]{static_plot_dyn()}} which
consume this output.
}
