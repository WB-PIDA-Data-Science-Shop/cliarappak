% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_plots.R
\name{static_map}
\alias{static_map}
\title{Static choropleth world map for one indicator (World Map tab)}
\usage{
static_map(
  source,
  var,
  title,
  selected,
  base_country,
  comparison_countries,
  spatial_data
)
}
\arguments{
\item{source}{One of \code{"raw"} (latest observed value, with the observation
year in the tooltip) or \code{"ctf"} (2020-2024 closeness-to-frontier average).}

\item{var}{Character. Indicator code (the \code{variable} column) -- used to pick
the \verb{value_*} / \verb{ctf_*} / \verb{year_*} columns out of \code{spatial_data}.}

\item{title}{Character. Plot title (usually the indicator display name).}

\item{selected}{Currently-selected indicator display name (passed through for
labelling).}

\item{base_country}{Character vector of base-country name(s) to outline.}

\item{comparison_countries}{Character vector of comparison-country names to
outline.}

\item{spatial_data}{An \code{sf} polygon layer (\code{app_data$spatial_data}) carrying
one \verb{value_<var>} / \verb{ctf_<var>} / \verb{year_<var>} column set per indicator.}
}
\value{
A \code{ggplot} object.
}
\description{
Builds the ggplot choropleth behind the World Map tab: every country shaded
by its value (or closeness-to-frontier) for a single indicator, with the base
and comparison countries outlined. Piped into \code{\link[=interactive_map]{interactive_map()}}.
}
\seealso{
\code{\link[=interactive_map]{interactive_map()}}, \code{\link[=check_spatial_data]{check_spatial_data()}}.
}
