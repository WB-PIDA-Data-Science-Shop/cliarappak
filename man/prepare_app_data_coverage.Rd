% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_app_data.R
\name{prepare_app_data_coverage}
\alias{prepare_app_data_coverage}
\title{Build the coverage-analysis long dataset for the Coverage Report}
\usage{
prepare_app_data_coverage(raw_data, db_variables, year_window = NULL)
}
\arguments{
\item{raw_data}{Compiled indicators data, same shape as
\code{\link[=build_app_data]{build_app_data()}}'s \code{raw_data} element (has a \code{Year} column, not
\code{year}).}

\item{db_variables}{Indicator metadata -- pass \code{cliaretl::db_variables}
(NOT \code{app_data$db_variables}, which \code{\link[=build_app_data]{build_app_data()}} pre-filters to
benchmarked/family-level variables; this needs the full table so every
indicator's \code{source}/\code{family_name}/\code{benchmarked_ctf} shows up in the
coverage report, not just the subset the interactive dashboard
benchmarks).}

\item{year_window}{Integer vector of years to include. Defaults to the 5
years ending the year before \code{db_variables}'s \code{ref_year} attribute.}
}
\value{
A long-format tibble: one row per country x indicator x year,
with \code{indicator_value}, and \code{var_name}/\code{source}/\code{family_name}/
\code{benchmarked_ctf} metadata joined in.
}
\description{
Ports \code{cliarapp/source/data_coverage_processing.R} -- previously a
one-off script, run manually each data-release cycle, that pivoted
\code{compiled_indicators.rds} to long format and joined in indicator metadata
to produce a static \code{coverage_ctf_for_analysis.csv} someone then had to
remember to copy into the app's data folder. Computing it live here means
it can never go stale. The historical "static 2020 to 2024 period" window
(the original script's own comment reads "2025 release, should take the
static 2020 to 2024 period") is now derived from \code{db_variables}'s own
\code{ref_year} attribute rather than a number a person had to remember to
bump by hand -- \code{ref_year = 2025} reproduces \code{2020:2024} exactly.
}
