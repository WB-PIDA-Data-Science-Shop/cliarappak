% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_plots.R
\name{static_scatter}
\alias{static_scatter}
\title{Scatter plot of two indicators across countries (Bivariate Correlation tab)}
\usage{
static_scatter(
  data,
  base_country,
  comparison_countries,
  high_group,
  y_scatter,
  x_scatter,
  variable_names,
  country_list,
  linear_fit,
  color_base_scatter,
  color_comp_scatter
)
}
\arguments{
\item{data}{Wide indicator table (\code{app_data$global_data}).}

\item{base_country}{Character. Base-country name.}

\item{comparison_countries}{Character vector of comparison-country names.}

\item{high_group}{Data frame of countries to highlight (a group), possibly
zero-row; from \code{mod_bivariate_server()}'s \code{high_group} reactive.}

\item{y_scatter, x_scatter}{Character. Indicator display names for the two
axes, or the literal \code{"Log GDP per capita, PPP"}.}

\item{variable_names}{Indicator dictionary (\code{app_data$variable_names}).}

\item{country_list}{Country/group metadata (\code{app_data$country_list}).}

\item{linear_fit}{Logical. Whether to add an OLS fit line.}

\item{color_base_scatter, color_comp_scatter}{Hex colour strings for the
base-country and comparison-country points.}
}
\value{
A named list: \code{sc_plot} (a \code{ggplot}) and \code{sc_data} (the tibble
plotted).
}
\description{
Builds the ggplot behind the Bivariate Correlation tab: one point per country
positioned by its closeness-to-frontier on the chosen x and y indicators
(either axis may instead be \code{"Log GDP per capita, PPP"}), with the base and
comparison countries highlighted and an optional linear fit. Returns both the
plot and the plotted data (the latter is offered as a CSV download).
}
\seealso{
\code{\link[=interactive_scatter]{interactive_scatter()}}, \code{\link[=x_scatter_choices]{x_scatter_choices()}},
\code{\link[=mod_bivariate_server]{mod_bivariate_server()}}.
}
