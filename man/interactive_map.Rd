% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_plots.R
\name{interactive_map}
\alias{interactive_map}
\title{Convert a world-map ggplot to an interactive plotly}
\usage{
interactive_map(x, var, definitions, buttons, source)
}
\arguments{
\item{x}{A \code{ggplot} object from \code{\link[=static_map]{static_map()}}.}

\item{var}{Character. Indicator code, used both to look up the definition row
in \code{definitions} and to name the export file.}

\item{definitions}{Indicator dictionary (\code{app_data$db_variables}) with
\code{variable}, \code{description}, \code{source} columns.}

\item{buttons}{Character vector of plotly modebar button ids to remove.}

\item{source}{One of \code{"raw"} or \code{"ctf"} (kept for parity with \code{\link[=static_map]{static_map()}};
currently only affects commented-out legend styling).}
}
\value{
A \code{plotly} htmlwidget.
}
\description{
Turns a \code{\link[=static_map]{static_map()}} ggplot into the plotly widget shown on the World Map
tab: hides the axes, adds the standard World Bank map disclaimer plus the
indicator definition/source/note as a footnote annotation, trims the modebar
and names the PNG export \verb{<var>_map}.
}
\seealso{
\code{\link[=static_map]{static_map()}}.
}
