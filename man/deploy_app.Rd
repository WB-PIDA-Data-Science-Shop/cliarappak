% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deploy_app.R
\name{deploy_app}
\alias{deploy_app}
\title{Deploy cliarappak to Posit Connect}
\usage{
deploy_app(
  onStart = NULL,
  options = list(),
  enableBookmarking = NULL,
  uiPattern = "/",
  dynamic_year_cutoff = NULL,
  type = c("dev", "prod"),
  server = "internal-server",
  forceUpdate = TRUE,
  ...
)
}
\arguments{
\item{onStart, options, enableBookmarking, uiPattern}{Forwarded to the
generated \code{run_app()} call. See \code{?shiny::shinyApp}.}

\item{dynamic_year_cutoff}{Integer year, or \code{NULL} (default) to derive one
via \code{\link[=resolve_dynamic_year_cutoff]{resolve_dynamic_year_cutoff()}} -- resolved once, here, at deploy
time, and written literally into the generated entry file so it survives
every future restart of the deployed content, not just this deploy.}

\item{type}{Character string specifying the deployment target. Must be
one of:
\describe{
\item{\code{"dev"}}{Deploys to the development slot.
Uses the \code{cliarappak_dev_guid} environment variable.}
\item{\code{"prod"}}{Deploys to the production slot.
Uses the \code{cliarappak_prod_guid} environment variable.}
}}

\item{server}{Name of the Posit Connect server, as registered with
\code{\link[rsconnect:addServer]{rsconnect::addServer()}}.}

\item{forceUpdate}{Passed to \code{\link[rsconnect:deployApp]{rsconnect::deployApp()}} -- skips the
interactive confirmation prompt on redeploys.}

\item{...}{Further named arguments to bake into the generated \code{run_app()}
call (forwarded to \code{golem_opts}, same as extra arguments to
\code{\link[=run_app]{run_app()}} itself).}
}
\value{
Invisibly returns the deployment information from
\code{\link[rsconnect]{deployApp}}.
}
\description{
Regenerates the app's Posit Connect entry file with the current
\code{\link[=run_app]{run_app()}} arguments baked in, then publishes it with
\code{\link[rsconnect:deployApp]{rsconnect::deployApp()}}.
}
\details{
This function requires an environment variable set with the Posit Connect
application GUID for the chosen \code{type}:
\itemize{
\item \code{cliarappak_dev_guid}
\item \code{cliarappak_prod_guid}
}
These can be set in your \code{.Renviron} file or through
\code{Sys.setenv()}.
}
\examples{
\dontrun{
# Preview a specific data vintage locally first...
run_app(dynamic_year_cutoff = 2022)

# ...then ship that exact vintage.
Sys.setenv(cliarappak_dev_guid = "your-guid-here")
deploy_app(dynamic_year_cutoff = 2022, type = "dev")

# Production deploy with the auto-derived, current-cycle cutoff.
Sys.setenv(cliarappak_prod_guid = "your-guid-here")
deploy_app(type = "prod")
}

}
