% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_quantiles.R
\name{missing_var}
\alias{missing_var}
\alias{missing_var_dyn}
\title{Display names of indicators the base country is missing}
\usage{
missing_var(
  data,
  base_country,
  country_list,
  comparison_countries,
  vars,
  variable_names
)

missing_var_dyn(
  data,
  base_country,
  country_list,
  comparison_countries,
  vars,
  variable_names
)
}
\arguments{
\item{data}{Long/wide CLIAR indicator table -- \code{app_data$global_data} for the
static variant, \code{app_data$global_data_dyn} for the dynamic one. First five
columns are identifiers (\code{country_name}, \code{country_code}, region/income
grouping, etc.); the remaining columns are one indicator each.}

\item{base_country}{Character. One or more country names. The country (or
countries) being benchmarked; drives both the missing-data and
low-variance screens.}

\item{country_list}{Data frame of country metadata with at least a
\code{country_name} column. Used to resolve \code{comparison_countries} to the set of
rows to keep. This is \code{app_data$country_list}.}

\item{comparison_countries}{Character vector of country names to benchmark
the base country against.}

\item{vars}{Character vector of indicator \emph{codes} (the \code{variable} column, not
the display name) to evaluate.}

\item{variable_names}{Indicator dictionary: a data frame keyed by \code{variable}
with \code{var_name} (display label) and \code{family_var} / family columns. This is
\code{app_data$variable_names}.}
}
\value{
Character vector of indicator display names (\code{var_name}) that are
missing for the base country. \code{missing_var_dyn()} is de-duplicated.
Returned invisibly.
}
\description{
\code{missing_var()} returns the human-readable names (\code{var_name}) of the
indicators in \code{vars} that the base country has no data for, so the Country
Benchmarking tab can list them in the plot notes ("the following indicators
are not considered because the base country has no information...").
}
\details{
\code{missing_var_dyn()} is the dynamic-dataset variant: an indicator counts as
missing only when it is \emph{100\%} missing for the base country, and the result
is de-duplicated.

Where \code{\link[=low_variance]{low_variance()}} returns indicator \emph{codes}, these functions return
\emph{display names}, resolved through \code{variable_names}. The two lists are
concatenated by the caller before being passed to \code{\link[=plot_notes_function]{plot_notes_function()}}.
Both functions return their value invisibly.
}
\seealso{
\code{\link[=low_variance]{low_variance()}}, \code{\link[=def_quantiles]{def_quantiles()}}, \code{\link[=plot_notes_function]{plot_notes_function()}}.
}
