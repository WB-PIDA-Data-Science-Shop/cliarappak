% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_plots.R
\name{clean_plotly_legend}
\alias{clean_plotly_legend}
\title{De-duplicate the legend of a facetted ggplotly}
\usage{
clean_plotly_legend(.pltly_obj, .new_legend = c())
}
\arguments{
\item{.pltly_obj}{A \code{plotly} object (typically straight out of
\code{plotly::ggplotly()}).}

\item{.new_legend}{Optional character vector of replacement legend-group
names, in order. Default \code{c()} keeps the cleaned original names.}
}
\value{
The \code{.pltly_obj}, with its \code{x$data} traces rewritten.
}
\description{
When a facetted ggplot is converted with \code{plotly::ggplotly()} every facet
contributes its own copy of each legend entry, so the legend repeats itself
N times. \code{clean_plotly_legend()} walks the plotly object's traces, strips the
\code{"(group,1)"} position suffixes ggplotly adds, collapses the duplicates, and
sets \code{showlegend = FALSE} on every trace after the first of each group.
}
\details{
Adapted from
\url{https://stackoverflow.com/questions/69289623/}. Defines three local
helpers -- \code{assign_leg_grp()}, \code{parse_leg_nms()} and \code{simplify_leg_grps()} --
that are implementation detail and not exported.
}
\seealso{
\code{\link[=interactive_plot]{interactive_plot()}}, \code{\link[=fixfacets]{fixfacets()}}.
}
