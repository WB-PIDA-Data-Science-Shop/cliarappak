% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_family.R
\name{family_data}
\alias{family_data}
\alias{family_data_dyn}
\title{Family-level closeness-to-frontier scores for the benchmarking plots}
\usage{
family_data(data, base_country, variable_names, comparison_countries)

family_data_dyn(data, base_country, variable_names)
}
\arguments{
\item{data}{Wide closeness-to-frontier table -- \code{app_data$global_data} for
\code{family_data()}, \code{app_data$global_data_dyn} for \code{family_data_dyn()}. First
few columns are identifiers; the rest are one indicator each.}

\item{base_country}{Character vector of base-country name(s); drives the
missing-data screen.}

\item{variable_names}{Indicator dictionary (\code{app_data$variable_names}) keyed
by \code{variable}, supplying \code{family_var}.}

\item{comparison_countries}{Character vector of comparison-country names
(\code{family_data()} only) -- used together with \code{base_country} for the
low-variance screen.}
}
\value{
A wide tibble: one row per \code{country_name} (per \code{country_name} and
\code{year} for \code{family_data_dyn()}), one column per institutional family, values being
the mean closeness-to-frontier of that family's indicators.
}
\description{
\code{family_data()} collapses the indicator-level closeness-to-frontier table to
one score per institutional family (the simple mean of the family's
indicators), for the static (single latest period) dataset. \code{family_data_dyn()}
does the same per year for the dynamic dataset. Both feed the "Overview" view
of the Country Benchmarking tab.
}
\details{
Before averaging, indicators are dropped if:
\itemize{
\item the base country has no data for them (\code{family_data()}: \emph{any} \code{NA};
\code{family_data_dyn()}: \emph{100\%} \code{NA}), or
\item \code{family_data()} only: they are flat across the base + comparison countries
(\code{\link[=check_quantiles]{check_quantiles()}} returns \code{TRUE}).
}

Family-average columns (names containing \verb{_avg}) and the \code{country_code}
column are also removed. The remaining indicators are pivoted long, joined to
\code{variable_names} for their \code{family_var}, averaged within
\code{country_name} (and \code{year}, for \verb{_dyn}), and pivoted back wide with one column
per family. \code{NaN} means (a family with no observed indicators) become \code{NA}.
}
\seealso{
\code{\link[=compute_family_average_app]{compute_family_average_app()}} for the \verb{_avg} columns used
elsewhere; \code{\link[=def_quantiles]{def_quantiles()}}; \code{\link[=static_plot]{static_plot()}}.
}
